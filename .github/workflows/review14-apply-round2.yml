name: Apply File 25 Review 14 Round 2

on:
  push:
    branches:
      - feature/25a-25b-foundation
    paths:
      - '.review14/round2.patch'
      - '.github/workflows/review14-apply-round2.yml'

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          ref: feature/25a-25b-foundation
          fetch-depth: 0
      - name: Apply clinic ownership correction
        shell: bash
        run: |
          set -euo pipefail
          git apply --check .review14/round2.patch
          git apply .review14/round2.patch
          php -l includes/class-native-integration.php
      - name: Commit correction and remove applicator
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add includes/class-native-integration.php
          git rm .review14/round2.patch .github/workflows/review14-apply-round2.yml
          git commit -m 'File 25 review 14 round 2: preserve File 08 clinic ownership [skip ci]'
          git push origin HEAD:feature/25a-25b-foundation
